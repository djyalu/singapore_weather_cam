name: Traffic Camera Monitor (Disabled)

# Webcam functionality has been replaced with real-time traffic camera system
# This workflow is disabled but kept for reference

on:
  # Disabled scheduled runs - webcam functionality replaced with real-time traffic cameras
  # schedule:
  #   - cron: '15 */3 * * *'
  workflow_dispatch:
    inputs:
      notice:
        description: 'This workflow has been disabled'
        type: choice
        options: [disabled]
        default: disabled

env:
  NODE_ENV: production
  WORKFLOW_DISABLED: 'true'

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    permissions:
      contents: read

    steps:
      - name: Webcam functionality disabled
        run: |
          echo "🚗 Webcam functionality has been replaced with real-time traffic camera system"
          echo "📱 Traffic cameras provide better real-time monitoring via data.gov.sg API"
          echo "✅ 90 traffic cameras are available through the web interface"
          echo "🔄 No webcam capture needed - real-time API provides live images"
          echo ""
          echo "This workflow has been disabled to prevent unnecessary resource usage."
          echo "The Singapore Weather Cam application now uses:"
          echo "  - Real-time LTA traffic camera API (90 cameras)"
          echo "  - Live weather data from NEA Singapore"
          echo "  - Interactive map with real-time updates"